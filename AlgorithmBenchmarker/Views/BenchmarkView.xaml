<UserControl x:Class="AlgorithmBenchmarker.Views.BenchmarkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:AlgorithmBenchmarker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="30">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel MaxWidth="700" HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="Benchmark Configuration" FontSize="28" FontWeight="Bold" Margin="0,0,0,25"/>

                <!-- Algorithm Selection -->
                <GroupBox Header="Algorithm Selection" Margin="0,0,0,20" Padding="15">
                    <StackPanel Margin="5">
                        <TextBlock Text="Category" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding Categories}" 
                                  SelectedItem="{Binding SelectedCategory}" 
                                  IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInverseConverter}}"
                                  Margin="0,0,0,20"/>

                        <TextBlock Text="Algorithm" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <ComboBox ItemsSource="{Binding Algorithms}" 
                                  SelectedItem="{Binding SelectedAlgorithm}" 
                                  DisplayMemberPath="Name"
                                  IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInverseConverter}}"/>
                    </StackPanel>
                </GroupBox>

                <!-- Configuration -->
                <GroupBox Header="Settings" Margin="0,0,0,25" Padding="15">
                    <StackPanel Margin="5">
                        <TextBlock Text="Input Size Range" FontWeight="Bold" Margin="0,0,0,12"/>
                        
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Min Size" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Config.MinInputSize}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="10,0,10,0">
                                <TextBlock Text="Max Size" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Config.MaxInputSize}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                                <TextBlock Text="Step Size" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Config.StepSize}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Input Data Type" Margin="0,0,0,8"/>
                        <ComboBox ItemsSource="{Binding InputTypes}" 
                                  SelectedItem="{Binding Config.InputType}"
                                  Margin="0,0,0,20"/>

                        <TextBlock Text="Input Distribution" Margin="0,0,0,8"/>
                        <ComboBox ItemsSource="{Binding DistributionTypes}" 
                                  SelectedItem="{Binding Config.Distribution}"
                                  Margin="0,0,0,20"/>

                        <TextBlock Text="Repetitions (Average)" Margin="0,0,0,8"/>
                        <TextBox Text="{Binding Config.Repetitions}"/>
                    </StackPanel>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <!-- Status Message -->
                    <TextBlock Text="{Binding StatusMessage}" 
                               Foreground="{StaticResource AccentBrush}" 
                               VerticalAlignment="Center"
                               Margin="0,0,20,0"
                               TextWrapping="Wrap"
                               MaxWidth="300"
                               TextAlignment="Right"/>

                    <!-- Run Button -->
                    <Button Content="RUN BENCHMARK" 
                            Command="{Binding RunBenchmarkCommand}" 
                            IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInverseConverter}}"/>

                    <!-- Result Button (Pulsing) -->
                    <Button Content="VIEW RESULTS" 
                            Command="{Binding ViewResultsCommand}" 
                            Margin="15,0,0,0"
                            Background="#28A745"
                            Visibility="{Binding IsResultReady, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsResultReady}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="PulseStoryboard">
                                                <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="1.0" To="0.6" Duration="0:0:0.8"/>
                                                    <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                                        From="15,0,0,0" To="15,-2,0,2" Duration="0:0:0.8"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="PulseStoryboard"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
