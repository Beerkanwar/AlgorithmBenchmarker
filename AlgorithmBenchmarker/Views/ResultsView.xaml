<UserControl x:Class="AlgorithmBenchmarker.Views.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:viewmodels="clr-namespace:AlgorithmBenchmarker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:ResultsViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/> <!-- Dynamic resize -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Refresh DB" Command="{Binding LoadResultsCommand}" Margin="0,0,10,0"/>
            <Button Content="Reset Graph" Command="{Binding ClearResultsCommand}" Margin="0,0,10,0" Background="#C42B1C"/>
            <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Margin="0,0,10,0"/>
            <Button Content="Export JSON" Command="{Binding ExportJsonCommand}" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- Time Chart -->
        <GroupBox Header="Execution Time Complexity (Ms vs Input Size)" Grid.Row="1">
            <lvc:CartesianChart Series="{Binding TimeSeries}" 
                                XAxes="{Binding XAxes}" 
                                YAxes="{Binding YAxesTime}"
                                ZoomMode="Both"
                                LegendPosition="Right"
                                TooltipPosition="Center"/>
        </GroupBox>

        <!-- Memory Chart -->
        <GroupBox Header="Memory Complexity (Bytes vs Input Size)" Grid.Row="2">
            <lvc:CartesianChart Series="{Binding MemorySeries}" 
                                XAxes="{Binding XAxes}" 
                                YAxes="{Binding YAxesMem}"
                                ZoomMode="Both"
                                LegendPosition="Right"
                                TooltipPosition="Center"/>
        </GroupBox>

        <!-- Data Grid -->
        <DataGrid Grid.Row="3" 
                  ItemsSource="{Binding Results}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Margin="0,5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Batch" Binding="{Binding BatchId}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Timestamp}"/>
                <DataGridTextColumn Header="Algorithm" Binding="{Binding AlgorithmName}"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}"/>
                <DataGridTextColumn Header="Input Size" Binding="{Binding InputSize}"/>
                <DataGridTextColumn Header="Time (ms)" Binding="{Binding AvgTimeMs, StringFormat=N4}"/>
                <DataGridTextColumn Header="Memory (Bytes)" Binding="{Binding MemoryBytes, StringFormat=N0}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
